# Pinned environment for the karst topology oracle.
#
# Every version here was resolved once inside the pinned base image and frozen, so a
# regeneration a year from now computes the same numbers as the golden files it is
# compared against. Installed with --no-deps against these exact pins: pip is not
# allowed to re-resolve anything, because a metric that silently changes with a
# transitive upgrade is worse than one that fails to install.
#
# Base image (pinned by digest in karstnet-oracle.mjs): python:3.11-slim
#   -> Python 3.11.16
#
# 3.11 rather than something newer because mplstereonet is unmaintained and resolves
# cleanly only on the older interpreter; karstnet imports it at module load, so it is a
# hard dependency even though nothing here draws a stereonet.

karstnet==1.2.5

# karstnet's own dependencies
matplotlib==3.11.1
mplstereonet==0.6.3
networkx==3.6.1
numpy==2.4.6
scipy==1.17.1

# transitive
contourpy==1.3.3
cycler==0.12.1
fonttools==4.64.0
kiwisolver==1.5.1
packaging==26.3
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
